node_modules/
dist/
.memory/
.multi-memory/
*.db
*.db-shm
*.db-wal
.env*
!.env.example

# Generated by `arch-inventory` — a derived view of the tree, rebuilt on demand.
# Same treatment as multi-app: the inventory is an input to the portfolio brain,
# not a source artifact, and committing it would drift against the code it describes.
docs/
